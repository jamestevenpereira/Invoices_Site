name: Supabase Keep-Alive

on:
  schedule:
    - cron: '0 9 */5 * *'  # a cada 5 dias às 9h UTC
  workflow_dispatch:        # permite correr manualmente

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          echo "=== REST API (com chave) ==="
          curl -s \
            "https://rguwiaxjapotlsvpvgpt.supabase.co/rest/v1/" \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -w "\nHTTP Status: %{http_code}\n"

          echo "=== Auth Health (sem chave) ==="
          curl -s \
            "https://rguwiaxjapotlsvpvgpt.supabase.co/auth/v1/health" \
            -w "\nHTTP Status: %{http_code}\n"

          echo "=== REST root (sem chave) ==="
          curl -s \
            "https://rguwiaxjapotlsvpvgpt.supabase.co/" \
            -w "\nHTTP Status: %{http_code}\n"
